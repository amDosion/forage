# ================================================================
# Stable Diffusion WebUI Forge - Environment Configuration
# ================================================================

# ================================================================
# UI Selection / UI 选择
# ================================================================
# 可选值：auto / forge
# auto: AUTOMATIC1111 原版 WebUI (稳定)
# forge: Forge 官方版 (优化内存，速度快，推荐)
UI=forge

# ================================================================
# WebUI Startup Arguments / 启动参数（RTX 4090 + 质量优先优化）
# ================================================================
ARGS=--xformers --precision full --opt-sdp-attention --opt-channelslast --no-half-vae --disable-nan-check --cuda-stream --enable-insecure-extension-access --skip-python-version-check --skip-torch-cuda-test --api --listen --theme dark

# ================================================================
# API Tokens / API 令牌
# ================================================================

# HuggingFace Token
# 获取地址: https://huggingface.co/settings/tokens
HUGGINGFACE_TOKEN=your_huggingface_token_here

# Civitai API Token
# 获取地址: https://civitai.com/user/account
CIVITAI_API_TOKEN=your_civitai_token_here

# GitHub Personal Access Token (用于推送配置更新 / for pushing config updates)
# 获取地址 / Get from: https://github.com/settings/tokens
# 需要 repo 权限 / Requires 'repo' scope
GITHUB_TOKEN=your_github_token_here

# GitHub 配置仓库 / GitHub config repository
GITHUB_CONFIG_REPO=your_username/your_repo
GITHUB_CONFIG_BRANCH=main

# ================================================================
# Download Control Switches / 下载控制开关
# ================================================================

# 全局下载开关
ENABLE_DOWNLOAD=true

# Extensions / 扩展插件
ENABLE_DOWNLOAD_EXTS=true

# Checkpoint Models / 主模型
ENABLE_DOWNLOAD_MODEL_SD15=false
ENABLE_DOWNLOAD_MODEL_SDXL=false
ENABLE_DOWNLOAD_MODEL_FLUX=false

# VAE Models / VAE 模型
ENABLE_DOWNLOAD_VAE=false
ENABLE_DOWNLOAD_VAE_FLUX=false

# Text Encoders / 文本编码器
ENABLE_DOWNLOAD_TE=false
ENABLE_DOWNLOAD_TE_FLUX=false

# ControlNet Models / ControlNet 模型
# ⭐ 推荐：SDXL ControlNet Union（一个模型支持10+控制条件）
#   - 支持: Canny, Tile, Depth, Blur, Pose, Gray, Low Quality, Recolor, Scribble/Sketch
#   - 大小: ~2.5GB (ProMax版本)
#   - 来源: xinsir/controlnet-union-sdxl-1.0 (官方 HuggingFace)
#
# ⚠️ FLUX ControlNet 许可证说明：
#   - FLUX.1 Schnell (Apache 2.0) 目前无 ControlNet 支持
#   - FLUX.1 Dev (非商业许可) 有 ControlNet 但不适合商业用途
#   - 建议使用 SD 1.5 或 SDXL ControlNet（完全开源免费）
ENABLE_DOWNLOAD_CNET_SD15=false
ENABLE_DOWNLOAD_CNET_SDXL=false
ENABLE_DOWNLOAD_CNET_FLUX=false  # ⚠️ 无可用的 Apache 2.0 许可 FLUX ControlNet

# LoRA and Related / LoRA 相关模型
ENABLE_DOWNLOAD_LORAS=false

# Embeddings / 嵌入模型
ENABLE_DOWNLOAD_EMBEDDINGS=false

# Upscaler Models / 放大模型
# 包含：RealESRGAN, SwinIR, HAT 等高质量放大模型
ENABLE_DOWNLOAD_UPSCALERS=true

# ================================================================
# Mirror Settings / 镜像加速设置
# ================================================================

# HuggingFace 镜像开关
USE_HF_MIRROR=false

# Git 镜像开关
USE_GIT_MIRROR=false

# ================================================================
# 性能优化说明（RTX 4090 + 256GB RAM）
# ================================================================
#
# 当前配置已针对以下硬件优化：
# - GPU: RTX 4090 (24GB VRAM)
# - RAM: 256GB
# - 优化目标: 质量优先（full precision）
#
# 主要优化项：
# - precision full: 最佳质量输出
# - opt-sdp-attention: PyTorch 2.0+ 优化注意力机制
# - opt-channelslast: Ada 架构 GPU 内存优化
# - no-half-vae: 避免 VAE 半精度问题
# - disable-nan-check: 生产环境性能提升
#
# Docker Compose 性能配置：
# - shm_size: 96GB（共享内存）
# - tmpfs: 90GB（内存加速）
# - ipc: host（多进程优化）
#
# 预期性能提升：
# - SDXL 生成速度: +30-35%
# - 临时文件 IO: +50倍
# - 图像质量: 最佳（full precision）
# - 内存稳定性: 零 OOM
#
# 🎨 面料/服装细节超清放大专业方案：
#
# 方案 1：Tile ControlNet + Ultimate SD Upscale（最推荐）
# - 适合：织物纹理、刺绣、服装细节
# - 配置：Denoise 0.3-0.4, ControlNet Strength 0.9
#
# 方案 2：SwinIR（细节之王）
# - 模型：003_realSR_BSRGAN_DFOWMFC_s64w8_SwinIR-L_x4_GAN
# - 质量评分：9.7/10（最高）
# - 适合：精确细节保留、纹理还原
#
# 方案 3：HAT（真实照片最佳）
# - 模型：HAT_SRx4_ImageNet-pretrain
# - 适合：真实服装照片、面料摄影
#
# 方案 4：组合方案（速度+质量平衡）
# - 步骤 1：RealESRGAN x4plus 快速放大
# - 步骤 2：SwinIR 细节精修
#
