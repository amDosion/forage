# ================================================================
# Stable Diffusion WebUI Forge - Environment Configuration Template
# ================================================================
# 复制此文件为 .env 并填入你的配置值
# Copy this file to .env and fill in your configuration values

# ================================================================
# UI Selection / UI 选择
# ================================================================
# 可选值 / Options: forge | auto | fastforge
# - forge: Stable Diffusion WebUI Forge (推荐 / recommended)
# - auto: AUTOMATIC1111 WebUI
# - fastforge: Fast Forge (实验性 / experimental)
UI=forge

# ================================================================
# WebUI Startup Arguments / 启动参数
# ================================================================
# 默认参数已包含 xformers, API, 远程访问等
# Default args include xformers, API, remote access, etc.
ARGS=--xformers --precision autocast --cuda-malloc --cuda-stream --enable-insecure-extension-access --skip-python-version-check --skip-torch-cuda-test --api --listen --theme dark

# ================================================================
# API Tokens / API 令牌
# ================================================================

# HuggingFace Token (用于下载受限模型 / for downloading gated models)
# 获取地址 / Get from: https://huggingface.co/settings/tokens
# 留空则跳过 HF 登录 / Leave empty to skip HF login
HUGGINGFACE_TOKEN=

# Civitai API Token (用于下载 Civitai 模型 / for downloading Civitai models)
# 获取地址 / Get from: https://civitai.com/user/account
# 留空则无法下载需要登录的模型 / Leave empty if not using Civitai
CIVITAI_API_TOKEN=

# GitHub Personal Access Token (用于推送配置更新 / for pushing config updates)
# 获取地址 / Get from: https://github.com/settings/tokens
# Fine-grained token 需要以下权限 / Fine-grained token requires:
#   - Repository access: 选择你的配置仓库 / Select your config repository
#   - Contents: Read and write
# Classic token 需要 repo 权限 / Classic token requires 'repo' scope
# 留空则无法自动推送配置文件到远程仓库 / Leave empty to disable auto-push
GITHUB_TOKEN=

# GitHub 配置仓库 / GitHub config repository
# 格式 / Format: owner/repo
# 修改为你自己的仓库 / Change to your own repository
GITHUB_CONFIG_REPO=amDosion/forage
GITHUB_CONFIG_BRANCH=main

# ================================================================
# Download Control Switches / 下载控制开关
# ================================================================

# 全局下载开关 / Global download switch
# true = 启用所有下载 / enable all downloads
# false = 禁用所有下载 / disable all downloads (除非单独指定 / unless individually specified)
ENABLE_DOWNLOAD=true

# ----------------------------------------------------------------
# Extensions / 扩展插件
# ----------------------------------------------------------------
# 是否下载/更新扩展插件 / Whether to download/update extensions
ENABLE_DOWNLOAD_EXTS=true

# ----------------------------------------------------------------
# Checkpoint Models / 主模型
# ----------------------------------------------------------------
# SD 1.5 Checkpoint 模型 / SD 1.5 Checkpoint models
ENABLE_DOWNLOAD_MODEL_SD15=false

# SDXL Checkpoint 模型 / SDXL Checkpoint models
ENABLE_DOWNLOAD_MODEL_SDXL=false

# FLUX Checkpoint 模型 / FLUX Checkpoint models
ENABLE_DOWNLOAD_MODEL_FLUX=false

# ----------------------------------------------------------------
# VAE Models / VAE 模型
# ----------------------------------------------------------------
# 通用 VAE 模型 / General VAE models
ENABLE_DOWNLOAD_VAE=false

# FLUX 专用 VAE / FLUX-specific VAE
ENABLE_DOWNLOAD_VAE_FLUX=false

# ----------------------------------------------------------------
# Text Encoders / 文本编码器
# ----------------------------------------------------------------
# 通用文本编码器 / General text encoders
ENABLE_DOWNLOAD_TE=false

# FLUX 专用文本编码器 / FLUX-specific text encoders
ENABLE_DOWNLOAD_TE_FLUX=false

# ----------------------------------------------------------------
# ControlNet Models / ControlNet 模型
# ----------------------------------------------------------------
# SD 1.5 ControlNet 模型 / SD 1.5 ControlNet models
ENABLE_DOWNLOAD_CNET_SD15=false

# SDXL ControlNet 模型 / SDXL ControlNet models
ENABLE_DOWNLOAD_CNET_SDXL=false

# FLUX ControlNet 模型 / FLUX ControlNet models
ENABLE_DOWNLOAD_CNET_FLUX=false

# ----------------------------------------------------------------
# LoRA and Related / LoRA 相关模型
# ----------------------------------------------------------------
# LoRA / LyCORIS / LoCon 模型 / LoRA / LyCORIS / LoCon models
ENABLE_DOWNLOAD_LORAS=false

# ----------------------------------------------------------------
# Embeddings / 嵌入模型
# ----------------------------------------------------------------
# Textual Inversion / Embeddings 模型 / Textual Inversion / Embeddings
ENABLE_DOWNLOAD_EMBEDDINGS=false

# ----------------------------------------------------------------
# Upscaler Models / 放大模型
# ----------------------------------------------------------------
# ESRGAN / RealESRGAN 等放大模型 / ESRGAN / RealESRGAN upscaler models
ENABLE_DOWNLOAD_UPSCALERS=false

# ================================================================
# Mirror Settings / 镜像加速设置
# ================================================================

# HuggingFace 镜像开关 / HuggingFace mirror switch
# true = 使用 hf-mirror.com 加速 / use hf-mirror.com for acceleration
# false = 使用官方源 / use official source
USE_HF_MIRROR=false

# Git 镜像开关 / Git mirror switch
# true = 使用 gitcode.net 加速 GitHub / use gitcode.net for GitHub acceleration
# false = 使用官方 GitHub / use official GitHub
USE_GIT_MIRROR=false

# ================================================================
# Advanced Settings / 高级设置 (一般无需修改 / usually no need to modify)
# ================================================================

# PyTorch Index URL / PyTorch 索引地址
# PIP_EXTRA_INDEX_URL=https://download.pytorch.org/whl/cu128

# PyTorch Install Command / PyTorch 安装命令
# TORCH_COMMAND=pip install torch==2.7.0+cu128 --extra-index-url https://download.pytorch.org/whl/cu128

# Disable TCMalloc / 禁用 TCMalloc
# NO_TCMALLOC=1

# Disable AWS EC2 Metadata / 禁用 AWS 元数据检测
# AWS_EC2_METADATA_DISABLED=true
